@model IEnumerable<WorldOfWarshipsAcademy.Models.ShipClasses>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<p> <a asp-action="Index" asp-controller="Destroyers">Destroyers</a> </p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ShipClass)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ShipClass)
            </td>
            <td>            
                <a asp-action="Details" asp-route-id="@item.Abbreviation">Details</a> |            
            </td>
        </tr>
}
    </tbody>
</table>
